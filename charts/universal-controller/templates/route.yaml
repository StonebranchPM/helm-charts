{{- $isOCP := include "uc.isOpenShift" . | eq "true" -}}
{{- if and $isOCP .Values.route.enabled }}
{{- if .Values.oms.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Release.Name }}-oms-route
  namespace: {{ include "oms.namespace" . }}
  labels:
    {{- include "universal-controller.labels" . | nindent 4 }}
    app.kubernetes.io/component: oms-route
spec:
  to:
    kind: Service
    name: {{ .Release.Name }}-oms-service
  tls:
    termination: passthrough
    insecureEdgeTerminationPolicy: None
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Release.Name }}-udm-route
  namespace: {{ include "oms.namespace" . }}
  labels:
    {{- include "universal-controller.labels" . | nindent 4 }}
    app.kubernetes.io/component: udm-route
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  to:
    kind: Service
    name: {{ .Release.Name }}-oms-service
  port:
    targetPort: udm
  tls:
    termination: passthrough
    insecureEdgeTerminationPolicy: None
---
{{- end }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Release.Name }}-uc-route
  namespace: {{ include "uc.namespace" . }}
  labels:
    {{- include "universal-controller.labels" . | nindent 4 }}
    app.kubernetes.io/component: uc-route
spec:
  to:
    kind: Service
    name: {{ .Release.Name }}-uc-service
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: None
{{- end }}
